#include "main.h"
/**
 * print_last_digit - return last digit of given number
 * @n: number to determine last digit
 * Return: lastn
 */
int print_last_digit(int n)
{
	int lastn;

	lastn = n % 10;
	return (lastn);
}
